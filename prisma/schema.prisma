generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model PendingConfirmation {
  id           String   @id @default(cuid())
  chatId       String
  action       String
  intent       String?
  originalText String?
  expiresAt    Int
  createdAt    DateTime @default(now())
}

// Additional models can be added progressively (Clients, Forwards, ChatActions, etc.)
