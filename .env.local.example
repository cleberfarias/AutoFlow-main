# Example local env (do NOT commit your real keys)
# Copy to .env.local and replace values with your secrets

# BACKEND ONLY - Usada pelos endpoints serverless Vercel (pasta /api)
# Esta chave é usada pelo sistema de suporte hierárquico (/api/support-router.ts)
OPENAI_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Frontend (legacy - em processo de migração para backend)
# Será removido após migração completa para endpoints serverless
VITE_OPENAI_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Outros serviços
GEMINI_API_KEY="gx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
