# Example local env (do NOT commit your real keys)
# Copy to .env.local and replace values with your secrets

# ========================================
# BACKEND/SERVER - OPENAI API KEY (OBRIGATÓRIO)
# ========================================
# A chave da OpenAI deve estar SOMENTE no servidor (NUNCA no frontend)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-..."

# ========================================
# SERVER - PORTA (padrão: 5050)
# ========================================
PORT=5050

# ========================================
# DESENVOLVIMENTO LOCAL
# ========================================
# Para rodar com IA real:
#   1. Configure OPENAI_API_KEY acima
#   2. Execute: npm run dev:full (frontend + backend)
#
# Para testar SEM IA (modo mock):
#   1. NÃO configure OPENAI_API_KEY (ou deixe em branco)
#   2. Execute: npm run dev:full
#   3. O backend retornará respostas mock para desenvolvimento

# ========================================
# WHATSAPP (OPCIONAL)
# ========================================
SKIP_WHATSAPP=1  # Definir como 1 para desabilitar WhatsApp durante desenvolvimento
WHATSAPP_SESSION_DIR=/tmp/whatsapp-session
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# ========================================
# TESTE E CI/CD
# ========================================
NODE_ENV=development
